# Needed:
If zeroing on line `1652` missed tests fail:
```
diff-snes_tutorials-ex12_quad_hpddm_reuse_threshold_baij 
diff-snes_tutorials-ex12_quad_hpddm_reuse_threshold
```
Needed also in multiple RHS analougue?
If no `local` guard on `1640/1648` and  failes because get colum vec returns `nullptr`.

